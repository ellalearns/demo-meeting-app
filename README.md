# Meeting App (Minimal React + Webpack)

## Run
```bash
npm install
npm start
```
- App runs at http://localhost:3000
- Login with `admin@test.com` / `1234`

## Notes
- Minimal deps: React, ReactDOM, React Router + Webpack/Babel + HtmlWebpackPlugin.
- Client-side routing with protected routes.
